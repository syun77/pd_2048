# 入力制御とリプレイ設計

## 目的

メインゲームの更新処理が Playdate のボタン API に直接依存しないようにし、同じゲームロジックに対して実機入力・自動プレイ・リプレイを差し替えられるようにする。

## モジュール

- `source/input/input_command.lua`: `MOVE_LEFT`、`MOVE_RIGHT`、`DROP`、`HOLD`、`REWIND` の論理コマンドを定義する。
- `source/input/input_history.lua`: 受理した論理コマンドを順番に記録し、リプレイ時は同じ順番で再生する。履歴の単位はフレームではなく、ゲームが入力を受理したイベントである。
- `source/input/input_controller.lua`: 入力源を `poll(context)` に統一する。実機入力は Playdate API とカーソルリピートを変換し、自動プレイは `decide(context)`、リプレイは履歴キューを使う。

## `main.lua` への接続方針

`updateGame` の `pd.buttonJustPressed` 分岐を `InputController:poll` に置き換え、返されたコマンドを既存の `moveCursor`、`beginDrop`、`holdCurrentBlock`、`beginRewindHold` に振り分ける。`context` には少なくとも `phase`、`cursorX`、盤面の読み取り専用コピー、`nextValues`、`holdValue`、`score` を渡す。

`poll` と記録は分離し、コマンドは `beginDrop` などの「入力が実際に受理された地点」で `InputController:recordAccepted` を呼んで記録する。盤面が満杯で DROP が拒否された場合や HOLD 使用済みの場合は記録しない。実機用の `InputController` はカーソルのリピート1回を1つの MOVE コマンドに変換する。

## 決定性

入力だけで盤面を完全再現するには、乱数系列も再現可能である必要がある。正式なリプレイ保存では、ゲーム開始時の乱数 seed を保存して `TileGenerator` を seed 付き RNG にするか、生成されたブロック値を履歴に併記する。まずは入力履歴を使い、テストでは生成値を固定する。

巻き戻しは `REWIND` という1イベントとして記録する。長押し時間そのものはゲームの仕様ではないため、リプレイ側ではコマンドを受けた時点で `undoLastTurn` を実行できるよう、入力変換後の処理を分離する。

## 拡張

履歴を datastore に保存する場合は、`InputHistory:export()` の配列に version、seed、ゲームモード、コマンド列を付加する。再生終了時に余ったコマンドがないことを検証すると、仕様変更や乱数ずれを検出できる。
