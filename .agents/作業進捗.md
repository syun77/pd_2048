# 作業進捗

## PLAYBOOKエディターの検証失敗時終了修正

- 目的: `playbook_editor.py`で保存時のバリデーション失敗後もエディターを継続利用できるようにする。
- 状態: 完了
- 完了事項: バリデーション結果表示をmacOSネイティブ`messagebox.showerror`から、全エラーをスクロール表示するTkinter製モーダルダイアログへ置き換えた。保存・Validateの検証呼び出しへ例外ガードを追加し、失敗時のステータスを表示するようにした。
- 未完了事項: なし。
- 判断: 長い複数行メッセージをネイティブダイアログへ渡さず、アプリ内ダイアログで表示する。予期しない検証例外も保存処理から外へ伝播させず、編集内容を保持する。
- 変更ファイル: `.agents/作業進捗.md`、`仕様書/playbook_editor.py`、`仕様書/プレイブックエディター.md`
- 検証結果: Python構文と`git diff --check`成功。画像未指定の保存を実行し、保存結果が`False`、エディターと検証ダイアログが存続、編集中IDが保持され、ダイアログを閉じた後もフォーム編集できることをTkinter上で確認した。
- 次の手順: なし。

## PLAYBOOKエディター実装

- 目的: `仕様書/プレイブックエディター.md`に従うPython＋Tkinter製`仕様書/playbook_editor.py`を作成する。
- 状態: 完了
- 完了事項: ヒントとページの追加・複製・削除・並べ替え、ID・アンロック番号・日英テキスト編集、同一フォルダPNGの選択・プレビュー、全項目検証、JSONの新規・読込・保存・別名保存、未保存確認、前回ファイル復元を実装した。
- 未完了事項: ゲーム内PLAYBOOK表示、初期`playbooks.json`、説明画像、実績連動は別作業として未実装。
- 判断: 外部依存を増やさず、PNG寸法検証はヘッダー解析、画面プレビューはTkinterの`PhotoImage`で実装した。未完成フォームでも項目移動でき、保存時とValidate時に全エラーをまとめて表示する。未保存JSONでは最初に選んだ画像のフォルダを作業フォルダとして扱う。
- 変更ファイル: `.agents/作業進捗.md`、`仕様書/playbook_editor.py`、`仕様書/プレイブックエディター.md`、`仕様書/playdate_2048_ゲーム仕様書.md`
- 検証結果: Python構文、JSON正規化、アンロック番号、重複ID、画像名、PNGヘッダー・寸法上限の非GUIテストに成功。Tkinter GUIの起動とレイアウトを目視確認。ヒント・ページの追加、複製、削除、並べ替えのGUIテストに成功。実PNGを用いたJSON保存・再読込の往復テストに成功。`git diff --check`成功。
- 次の手順: エディターで初期`playbooks.json`を作成し、説明画像を配置する。

## PLAYBOOK項目・データ・エディタ設計

- 目的: PLAYBOOKのヒント一覧、複数ページ表示、実績連動アンロック、`playbooks.json`、`playbook_editor.py`の仕様を設計する。
- 状態: 完了
- 完了事項: PLAYBOOK一覧・詳細画面の入力と表示、言語フォールバック、JSONスキーマ、画像制約、実績連動、エディタ画面・操作・検証・設定を設計した。初期ヒントとして`基本の遊び方`4ページ、`コンボの作り方`3ページ、`高い数を作る`3ページの日英説明案を定義した。
- 未完了事項: `playbooks.json`、説明画像、PLAYBOOK一覧・詳細表示、アンロック保存、`playbook_editor.py`の実装は未着手。
- 判断: 実績配列の並べ替えで対応が壊れないよう、アンロック番号は実績の表示順ではなく`PLAYBOOK_UNLOCK`報酬IDと照合する独立番号とした。同じ番号を複数ヒントへ設定し、1報酬で関連項目をまとめて解放できる。未解放項目は一覧から隠す。
- 変更ファイル: `.agents/作業進捗.md`、`仕様書/プレイブックエディター.md`、`仕様書/playdate_2048_ゲーム仕様書.md`、`仕様書/実績エディター.md`
- 検証結果: 既存のゲームルール、画面遷移、実績報酬形式と照合し、`git diff --check`に成功した。文書のみの変更のため`pdc`ビルドは実施していない。
- 次の手順: `playbook_editor.py`と初期`playbooks.json`を実装し、説明画像の制作後にゲーム内ローダー・一覧・詳細画面・アンロック保存を実装する。

## 今回の作業

- 目的: タイトル画面から選択できる空の`PLAYBOOK`画面を追加し、Bボタンでタイトルへ戻れるようにする。
- 状態: 完了
- 完了事項: タイトルROOTメニューへ`PLAYBOOK`を追加し、独立した`PLAYBOOK`シーンへ遷移するようにした。画面は`PLAYBOOK`見出しだけを描画し、Bボタンまたはシステムメニューの`Back to Title`でタイトルへ戻る。メニュー背景の対象画面へ追加し、ゲーム仕様書も更新した。
- 未完了事項: Simulatorへの自動キー送信がmacOSのAccessibility権限で拒否されたため、PLAYBOOKへの決定操作とBボタン復帰の動的確認は未実施。
- 判断: 将来の内容追加に備えてタイトル内サブページではなく独立シーンとした。空画面でも画面種別が分かるよう、見出しだけは表示する。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/app.lua`、`pd_2048/source/game_config.lua`、`pd_2048/source/render/title_renderer.lua`、`pd_2048/source/scene/scene_manager.lua`、`pd_2048/source/scene/scene_playbook.lua`、`pd_2048/source/scene/scene_title.lua`、`仕様書/playdate_2048_ゲーム仕様書.md`
- 検証結果: `git diff --check`成功。`pdc pd_2048/source pd_2048/builds`成功。Simulatorで生成した`source.pdx`を起動し、タイトルメニューへの`PLAYBOOK`表示とコンソールに実行時エラーがないことを確認した。
- 次の手順: 必要に応じてSimulator上で`PLAYBOOK`を決定し、Bボタンでタイトルへ戻る操作を手動確認する。

## 現在の作業

- 目的: リプレイ再生の一時停止地点から、永続記録対象外のプレイヤー操作へ分岐できるようにする。
- 状態: 完了

## 完了事項

- Aで一時停止、停止中のAで確認ダイアログ、`YES`でプレイヤー操作へ分岐する入力仕様を定義した。
- `NO`初期選択、上下選択、A決定、Bキャンセルと、ダイアログ中の入力排他を定義した。
- 安定状態だけでなく、待ち時間・HOLD・DROP・マージ・回転などの途中状態もそのまま引き継ぐ分岐規則を定義した。
- 再生状態と永続記録資格を分離し、分岐後もリプレイ、各種記録、統計、中断データ、将来の実績と報酬を保存しない方針を定義した。
- ゲーム仕様書と入力・リプレイ設計書を更新した。
- `game_state.lua`へ追加する状態と、既存の記録資格を共通の`persistentRecordEligible`へ統合する方針を定義した。
- `game_controller.lua`の分岐関数、入力優先順、再生専用状態の破棄順、永続記録ガードを定義した。
- `overlay_renderer.lua`、`game_renderer.lua`、`scene_game_normal.lua`など、描画とシーンの改修方法を定義した。
- 実装後に確認する分岐地点とdatastore非更新の検証項目を定義した。
- リプレイ一時停止中のAで確認ダイアログを開き、上下選択、A決定、Bキャンセルを処理する入力状態遷移を実装した。
- `YES`決定時に再生専用状態だけを破棄し、盤面、NEXT、HOLD、乱数、UNDO、進行中アニメーションを保持してプレイヤー入力へ切り替える処理を実装した。
- 分岐後にAuto Playとリプレイ記録を停止し、Retryでは通常の新規NORMALへ移行するようにした。
- `persistentRecordEligible`と`canWritePersistentProgress()`を追加し、統計、最高記録、中断保存、リプレイ保存の永続書き込みを共通で抑止した。
- `statisticsRunEligible`と`levelRecordEligible`を廃止し、中断データ形式をバージョン2へ更新した。
- 確認ダイアログ、一時停止操作案内、オーバーレイ優先順、システムメニュー更新を実装した。

## 未完了事項

- Simulatorでの分岐操作とdatastore非更新の動的確認は未実施。Simulatorへの自動キー送信はmacOSのAccessibility権限不足で実行できなかった。
- 前回実装したリプレイ早送りのSimulator確認も未実施。

## 判断・注意事項

- 分岐時にseedから再構築せず、現在の論理状態・乱数状態・UNDO・進行中アニメーションを保持する。
- 未適用のリプレイイベントだけを破棄し、すでに受理済みの操作は取り消さない。
- 分岐後のラン内スコアやレベルは通常どおり進行するが、永続記録資格はラン終了まで復活させない。
- 分岐後は中断保存を許可せず、Retryは新しい記録対象NORMALを開始する。
- 永続記録資格はREWINDで復活させないため、UNDOスナップショットへ含めない。
- 分岐後のゲームオーバーでは`finishReplayRecordingIfNeeded()`がチェックサム検証・中断データ削除・リプレイ保存を行わず、`REPLAY NOT SAVED`を表示しないようにする。
- 以前からの無関係な差分は変更していない。

## 変更ファイル

- `.agents/作業進捗.md`
- `仕様書/playdate_2048_ゲーム仕様書.md`
- `仕様書/入力制御とリプレイ設計.md`
- `pd_2048/source/game_config.lua`
- `pd_2048/source/game_state.lua`
- `pd_2048/source/game/game_controller.lua`
- `pd_2048/source/game/level_progress.lua`
- `pd_2048/source/input/replay_controller.lua`
- `pd_2048/source/render/overlay_renderer.lua`
- `pd_2048/source/render/game_renderer.lua`
- `pd_2048/source/scene/scene_game_normal.lua`

## 検証結果

- 関連するリプレイ入力、手送り、記録抑止、中断保存、結果画面、システムメニューの現行コードを確認した。
- 仕様書内の「続きから通常プレイを開始する機能は提供しない」という旧記述を更新した。
- リプレイ分岐を統計履歴、全記録対象外、中断セーブ、システムメニューの各仕様へ反映した。
- `git diff --check`成功。文書のみの変更のため`pdc`ビルドは実施していない。
- リプレイ再生、通常入力、統計、最高記録、リプレイ完了、中断復元、UNDO、結果画面、システムメニューのLua呼び出し経路を確認した。
- Lua実装方針の追記後に`git diff --check`成功。旧入力案内と旧「続きから開始不可」記述が残っていないことを確認した。
- `statisticsRunEligible`、`levelRecordEligible`、旧`A/B: RESUME`表示がLuaに残っていないことを確認した。
- `pdc pd_2048/source pd_2048/builds`成功。
- Playdate Simulatorで`pd_2048/builds/source.pdx`を起動し、タイトル画面と`REPLAYS`項目が実行時エラーなく表示されることを確認した。
- `osascript`によるSimulatorの自動操作は「キー操作の送信は許可されません」で失敗したため、分岐ダイアログ以降は未確認。

## 次の手順

- 分岐前後、アニメーション途中、手送り後、ゲームオーバー、Retry、システムメニュー、記録非更新をSimulatorで確認する。

## 前回作業の継続確認

- リプレイ再生中、Bボタン押下中は最大3000msの記録済み操作待ちだけを省略し、アニメーション速度は変更しない実装まで完了している。
- `git diff --check`と`pdc pd_2048/source pd_2048/builds`は成功済み。Simulatorでの動的確認が残っている。
