# 作業進捗

## 現在の作業

- 目的: AGENTS.md に、開発中の破壊的変更、全面的な最新化、コメント方針、最終リファクタリングの指示を追加する。
- 状態: 完了

## 完了事項

- `.agents/AGENTS作成ルール.md`、既存の `AGENTS.md`、直前の作業進捗を確認した。
- 後方互換性より最良の現行設計を優先し、関連箇所を同じ作業ですべて最新化する規則を追加した。
- 変更履歴コメントを残さず、最新状態の意図だけをコメントする規則を追加した。
- 一連の実装後に必ずリファクタリングを行う工程を追加した。

## 未完了事項

- なし。

## 判断・注意事項

- 破壊的変更の推奨はプロジェクト内の設計、API、データ形式を対象とし、無関係なユーザーデータの削除を意味しない。
- リファクタリング後の状態をビルドと Simulator で検証する順序にした。

## 変更ファイル

- `AGENTS.md`
- `.agents/作業進捗.md`

## 検証結果

- 文書変更のみのため、ゲームのビルドは不要。
- `git diff --check` で文書差分に問題がないことを確認済み。

## 次の手順

- 次回の実装では、機能実装後にリファクタリングを行ってから検証する。
