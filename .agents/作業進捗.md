# 作業進捗

## TIME ATTACKモードのアンロック基盤

- 目的: 実績報酬でTIME ATTACK各モードを解放できる永続状態とタイトルメニュー表示制御を実装する。
- 状態: 完了
- 完了事項: datastoreキー`achievements`の報酬形式を実装する`AchievementStore`を追加し、TIME ATTACK報酬4種の読込・検証・保存・問い合わせAPIを実装した。モードと報酬IDの対応を`game_config.lua`へ集約し、タイトルでは解放済みモードだけをサブメニュー候補にし、1件以上ある場合だけTIME ATTACKルートを表示するようにした。仕様書へ実装状態を反映した。
- 未完了事項: 実績条件達成から`AchievementStore:unlockReward`を呼ぶ処理。REPLAYS、プレイ分岐、SOUND TEST、PLAYBOOKの報酬接続。
- 判断: 将来の実績実装と保存形式を共用するため、別のモード設定ではなくdatastoreキー`achievements`の`rewards.timeAttack`へ保存する。新規状態は全対象モード未解放とする。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/game/achievement_store.lua`、`pd_2048/source/game_config.lua`、`pd_2048/source/app.lua`、`pd_2048/source/scene/scene_context.lua`、`pd_2048/source/scene/scene_title.lua`、`仕様書/playdate_2048_ゲーム仕様書.md`、`仕様書/実績システム設計.md`
- 検証結果: `pdc pd_2048/source pd_2048/builds`と`git diff --check`に成功した。Simulatorで実行時エラーがないこと、新規未解放状態ではTIME ATTACKルートが非表示であること、検証用に`sprint64`と`sprint256`を解放するとTIME ATTACKルートが表示されることを目視確認した。検証用datastoreは削除せず`/tmp`へ退避し、元の未作成状態へ戻した。
- 次の手順: タイル・PRACTICE実績の達成判定を実装し、対応するTIME ATTACK報酬を`unlockReward`へ接続する。

## 実績ID・達成数条件・アンロック仕様の確定

- 目的: 実績JSONの永続ID、5・11・17件の自己包含条件、時間実績文言、形式versionと、実績報酬による機能解放仕様を確定する。
- 状態: 完了
- 完了事項: メタ実績3件と時間実績4件を条件が明確な永続IDへ変更した。達成数条件を候補自身を含む5・11・17件へ変更し、日英文言を同期した。時間実績4件の`in within`を修正し、JSONへ`version=1`、エディターへversion入出力・検証を追加した。モード・REPLAYS・プレイ分岐・SOUND TESTを実績報酬で解放する完成仕様と、複数達成時の1件ずつの連続通知を仕様書へ明記した。
- 未完了事項: Luaアプリケーション側の実績読込・判定・保存・通知・機能解放実装。
- 判断: 複数同時達成は既存設計どおり1件ずつ連続通知する。PRACTICE全クリア、PLAYBOOK報酬対応、回数系・スコア実績を含めない方針は変更しない。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/assets/achievement/achievements.json`、`仕様書/achievement_editor.py`、`仕様書/実績エディター.md`、`仕様書/実績システム設計.md`、`仕様書/playdate_2048_ゲーム仕様書.md`
- 検証結果: Python構文解析、JSON全18件のエディター検証、`version=2`の拒否、永続ID7件、達成数5・11・17、時間mode4件を確認した。旧ID・旧説明・`in within`が実績関連文書とデータに残っていないこと、`jq`、`git diff --check`、`pdc pd_2048/source pd_2048/builds`の成功を確認した。
- 次の手順: 実績システム設計に従ってLua側を実装する。

## 実績エディターと実績JSONの正規化

- 目的: 実績システム設計に合わせ、`achievement_editor.py`と`achievements.json`だけを実装可能な定義へ修正する。
- 状態: 完了
- 完了事項: エディターの既定候補を6 Category、6 Condition Type、6 Reward Typeへ現行化した。条件別のCategory、Scope、Params型・キー・値と報酬IDを検証する処理を追加した。JSONから予約条件を除き、`HINT_UNLOCK`を`PLAYBOOK_UNLOCK`へ、報酬IDを`1`～`3`へ変更し、時間実績4件へ対象modeを追加した。
- 未完了事項: Luaアプリケーション側の実績読込・判定・保存・通知実装。
- 判断: 現行18件で使用する6 Condition Typeだけを選択可能にし、条件の意味をIDや表示名へ依存させない。数を表す条件値は正の整数、クリア時間の分数値は正の数として検証する。
- 変更ファイル: `.agents/作業進捗.md`、`仕様書/achievement_editor.py`、`pd_2048/source/assets/achievement/achievements.json`
- 検証結果: Pythonの構文解析、既定新規実績、現行JSON全18件の検証に成功した。時間mode欠落・不一致、小数コンボ、LEVELのScope不一致、不正PLAYBOOK報酬ID、NONEへの不要IDが拒否されることを確認した。`jq`によるJSON構文・18件確認、`git diff --check`、`pdc pd_2048/source pd_2048/builds`に成功した。
- 次の手順: 実績システム設計に従い、Lua側の定義ローダー、永続ストア、実績管理を実装する。

## 実績達成判定のアプリケーション設計

- 目的: 現行の実績仕様、実績エディター、`achievements.json`を基に、アプリケーション側の達成判定・保存・報酬・画面上部通知を設計する。
- 状態: 完了
- 完了事項: 18件の条件とゲームイベントを対応付け、定義ローダー、実績管理、永続ストア、報酬、達成数連鎖、画面上部の英語通知キューを設計した。実績エディター仕様とゲーム仕様書から設計書を参照するよう更新した。
- 未完了事項: 設計の実装。`achievements.json`の`HINT_UNLOCK`から`PLAYBOOK_UNLOCK`への変更、PLAYBOOK報酬IDの数値化、時間実績への対象mode追加、エディターの意味検証追加。
- 判断: JSON定義を正本とし、表示番号は配列順、永続状態は安定した実績IDで保持する。ゲーム側は確定イベントだけを通知し、条件評価を`AchievementManager`へ集約する。達成と報酬は同じdatastore書込みで確定し、通知は保存成功後に表示する。
- 変更ファイル: `.agents/作業進捗.md`、`仕様書/実績システム設計.md`、`仕様書/実績エディター.md`、`仕様書/playdate_2048_ゲーム仕様書.md`
- 検証結果: `achievements.json`が18件、使用Condition Typeが設計対象の6種であることを`jq`で確認した。現行データの`HINT_UNLOCK`3件と時間条件4件のmode不足を確認し、必要な修正として設計へ明記した。Markdownの行末空白がないことと`git diff --check`成功を確認した。文書設計だけのため`pdc`ビルドは実施していない。
- 次の手順: 設計書の実装順に従い、まずJSONと実績エディターの検証を正規化する。

## PLAYBOOKヒントタイトル変更

- 目的: PLAYBOOKのヒント「長く生き残る」を日英とも「孤立ブロックを作らない」の意味へ変更する。
- 状態: 完了
- 完了事項: `survival_tactics`の日本語タイトルを「孤立ブロックを作らない」、英語タイトルを「AVOID ISOLATED TILES」へ変更した。
- 未完了事項: なし。
- 判断: ゲーム内の既存英語表記に合わせ、「ブロック」は`TILES`と訳す。別項目「長く生き残る2」は変更しない。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/assets/playbook/playbooks.json`
- 検証結果: JSON構文確認、`git diff --check`、`pdc pd_2048/source pd_2048/builds`に成功した。
- 次の手順: なし。

## PLAYBOOKエディターの画像プレビュー修正

- 目的: `playbook_editor.py`で読み込んだ画像が縦に潰れて表示される不具合を修正する。
- 状態: 完了
- 完了事項: `tk.Label`の`width` / `height`が画像設定後にピクセル単位となり、プレビュー高が9px相当へ縮む原因を確認した。固定ピクセル高の専用コンテナと寸法定数を追加した。
- 未完了事項: なし。
- 判断: 有効画像の上限に合わせてプレビュー高を128pxとし、画像の縦横比を変えない整数間引きだけを行う。
- 変更ファイル: `.agents/作業進捗.md`、`仕様書/playbook_editor.py`、`仕様書/プレイブックエディター.md`
- 検証結果: Python構文確認、`git diff --check`、`pdc pd_2048/source pd_2048/builds`に成功した。Tkinter GUIで`howto_1.png`を読み込み、プレビューラベルが500×132px、表示用`PhotoImage`が原寸の262×128pxであることを確認した。
- 次の手順: なし。

## SURVIVAL TACTICSヒント英訳

- 目的: `SURVIVAL_TACTICS`と`SURVIVAL_TACTICS2`へ追加された日本語説明を英訳する。
- 状態: 完了
- 完了事項: 追加された日本語6ページと、既存PLAYBOOKで使用している英語用語を確認し、仮置きされていた旧英語説明を日本語に対応する英文へ差し替えた。
- 未完了事項: なし。
- 判断: 既存表記に合わせ、ブロックは`tile`、中央の回転軸は`center pivot`と表現する。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/assets/playbook/playbooks.json`
- 検証結果: JSON構文確認、`playbook_editor.py`の全項目検証（12ヒント、エラー0件）、`git diff --check`、`pdc pd_2048/source pd_2048/builds`に成功した。
- 次の手順: なし。

## PLAYBOOKヒント説明欄の3行対応

- 目的: 英語のヒント説明が3行に折り返されても表示できるよう、PLAYBOOK詳細のテキスト欄を広げる。
- 状態: 完了
- 完了事項: 添付画像で56pxの文字描画領域で2行目に省略されることを確認した。Playdateの英語フォントの3行分に合わせ、説明領域を84px、文字描画領域を68pxへ再拡大し、画像領域も合計12px追加で調整した。仕様書へ最大3行表示を明記した。
- 未完了事項: なし。
- 判断: 画像は拡大縮小せず原寸表示を維持し、今後の画像自体の高さ調整を前提に、画像パネルと中心位置を上へ調整する。文字描画領域はフォント高と行送りを含む3行分の68pxとする。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/render/title_renderer.lua`、`仕様書/プレイブックエディター.md`
- 検証結果: 前回の56pxでは実機表示上2行で省略されることを添付画像で確認した。68pxへの再修正後、`git diff --check`と`pdc pd_2048/source pd_2048/builds`に成功した。
- 次の手順: なし。

## PLAYBOOKエディターのページ削除クラッシュ修正

- 目的: `playbook_editor.py`でヒントのページをDeleteした際にエディターがクラッシュする不具合を修正する。
- 状態: 完了
- 完了事項: 削除後のページ一覧・選択・編集フォームの一括更新を実装した。追加ログから、実際のクラッシュはmacOSネイティブAlert解放時の`GameControllerUI` `SIGTRAP`であることを確認した。`messagebox`依存を削除し、削除確認、最小項目数の通知、未保存確認、成功・エラー通知をTkinterウィジェットだけで構成するエディタ内ダイアログへ置き換えた。
- 未完了事項: なし。
- 判断: 選択状態修正は保持する。Python 3.14.6、Tk 9.0.3、macOS 26.5.2の組み合わせでネイティブAlertの解放がクラッシュしているため、エディタ内の確認・通知にOSネイティブAlertを使用しない。
- 変更ファイル: `.agents/作業進捗.md`、`仕様書/playbook_editor.py`、`仕様書/プレイブックエディター.md`
- 検証結果: Python構文確認に成功。macOSのTkinter GUIを実際に起動し、Tk製確認ダイアログを開閉しながら3ページから末尾ページを1ページまで連続削除した。その後、最後の1ページを削除しようとしてTk製通知が開き、データが1ページのまま保持されることを確認した。`messagebox`呼び出しが残っていないこと、`git diff --check`、`pdc pd_2048/source pd_2048/builds`の成功を確認した。
- 次の手順: なし。

## 日本語フォント適用範囲修正

- 目的: 日本語を表示するメニューだけに日本語フォントを適用し、メインゲームを含むその他の画面ではPlaydateのデフォルトフォントを使用する。
- 状態: 完了
- 完了事項: フォント切替条件へシーン判定を追加し、`ja`選択時もPLAYBOOKとACHIEVEMENTSだけ日本語フォントを使用するようにした。それ以外の全画面はPlaydateのデフォルトフォントを毎フレーム明示設定する。仕様書を現行動作へ更新した。作業中に再生成された現行フォント定義と画像テーブルでは、先に確認した定義数の不一致が解消されていることも確認した。
- 未完了事項: なし。
- 判断: ゲーム内で日本語を描画するPLAYBOOKとACHIEVEMENTSだけを日本語フォント対象とし、それ以外は毎フレーム既定フォントを明示設定する。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/app.lua`、`仕様書/playdate_2048_ゲーム仕様書.md`
- 検証結果: `git diff --check`と`pdc pd_2048/source pd_2048/builds`に成功した。言語保存値が`ja`の状態で、Simulatorの一時直接起動ビルドを使い、PLAYBOOK一覧では日本語フォント、タイトルとメインゲームではPlaydateデフォルトフォントが適用され、コンソールに実行時エラーがないことを目視確認した。
- 次の手順: なし。

## 言語切替機能

- 目的: タイトル画面のPlaydateシステムメニューから`language`の`en` / `ja`を選択し、実績画面とPLAYBOOK画面の表示言語を切り替えられるようにする。
- 状態: 完了
- 完了事項: `LanguageManager`を追加し、初回はPlaydate OS言語、以後はdatastoreへ保存した`en` / `ja`を使用するようにした。タイトル画面だけに`language`オプション項目を表示し、PLAYBOOKのJSONタイトル・説明・固定文言とACHIEVEMENTSの固定文言・戻りメニューへ反映した。日本語用`k8x12`フォントとシステムメニュー用拡張フォントを設定し、仕様書を更新した。
- 未完了事項: Simulator上でシステムメニューを開き、`en` / `ja`を実際に切り替える手動操作確認。
- 判断: 前回のリプレイSimulator確認とは独立した作業として追記し、既存差分を上書きしない。設定が未保存ならOS言語を初期値とし、保存後はOS言語変更よりゲーム内選択を優先する。フォント未収録の`盤`を避けるため、PLAYBOOKの該当4箇所は`ボード`へ統一した。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/language_manager.lua`、`pd_2048/source/app.lua`、`pd_2048/source/pdxinfo`、`pd_2048/source/system_menu_controller.lua`、`pd_2048/source/scene/scene_context.lua`、`pd_2048/source/scene/scene_title.lua`、`pd_2048/source/scene/scene_playbook.lua`、`pd_2048/source/scene/scene_achievements.lua`、`pd_2048/source/playbook_loader.lua`、`pd_2048/source/render/title_renderer.lua`、`pd_2048/source/assets/fonts/k8x12.fnt`、`pd_2048/source/assets/fonts/k8x12-table-16-18.png`、`pd_2048/source/assets/playbook/playbooks.json`、`仕様書/playdate_2048_ゲーム仕様書.md`、`仕様書/プレイブックエディター.md`
- 検証結果: `git diff --check`成功。PLAYBOOKと固定文言が使う日本語240文字すべてのフォント収録を確認。`pdc pd_2048/source pd_2048/builds`成功し、`assets/fonts/k8x12.pft`の収録を確認。Simulatorでビルド済みPDXを起動し、数秒間の実行でコンソールエラーがないことを確認した。
- 次の手順: Simulatorまたは実機でタイトルのシステムメニューを開き、選択値の保存、PLAYBOOK一覧・詳細、ACHIEVEMENTSの英日表示を手動確認する。

## PLAYBOOKゲーム内表示実装

- 目的: アプリケーションで`playbooks.json`を読み込み、PLAYBOOK一覧とヒント詳細を操作・表示できるようにする。
- 状態: 完了
- 完了事項: `playbooks.json`の読込・形式確認・日英ローカライズ・表示対象抽出を行うローダーを追加した。PLAYBOOK一覧の選択・スクロールと、詳細の画像キャッシュ・説明文・ページ番号・左右非循環切替を実装した。Bボタンとシステムメニューによる一覧・タイトルへの復帰も画面状態に合わせて切り替えた。仕様書を現行実装へ更新した。
- 未完了事項: 実績のゲーム内達成管理とアンロック番号の永続保存。各ページ固有画像への差し替え。
- 判断: 実績アンロック保存は未実装のため、現段階では`unlockNo=0`だけを表示する。詳細は左右で非循環ページ切替、Bで一覧へ戻し、一覧のBはタイトルへ戻す。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/playbook_loader.lua`、`pd_2048/source/scene/scene_playbook.lua`、`pd_2048/source/render/title_renderer.lua`、`仕様書/playdate_2048_ゲーム仕様書.md`、`仕様書/プレイブックエディター.md`
- 検証結果: `pdc pd_2048/source pd_2048/builds`に成功し、生成物へ`playbooks.json`と`dummy.pdi`が収録されることを確認した。Simulatorの一時直接起動ビルドで、実データによる8件の一覧・スクロール表示と、詳細のタイトル・ページ番号・画像・説明文・ページカーソルを目視確認し、実行時エラーがないことを確認した。
- 次の手順: 実績機能の実装時に取得済みアンロック番号の集合を`PlaybookLoader.loadVisible`へ渡す。各ページ用PNGが完成したらJSONの`image`を差し替える。

## PLAYBOOK初期ヒント作成

- 目的: `assets/playbook/playbooks.json`へ現行ゲーム仕様に基づくヒント項目を追加する。
- 状態: 完了
- 完了事項: テスト項目を、基本操作、マージ、落下条件、回転、HOLD・REWIND、NEXT、スコア・レベル、コンボ、高数値、生存術、各モードの11ヒント・40ページへ置き換えた。全ページに日英説明を設定し、`dummy`画像を参照した。
- 未完了事項: 各ページ固有の説明画像制作と差し替え。アンロック番号1～3を付与する実績報酬の決定。
- 判断: 基本情報8件は`unlockNo=0`、攻略情報はコンボ`1`、高数値`2`、生存術`3`とした。テスト用`playbook_001`は正式項目へ置き換え、残していない。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/assets/playbook/playbooks.json`、`仕様書/プレイブックエディター.md`
- 検証結果: `playbook_editor.py`の読み込み・全項目検証に成功（11ヒント、40ページ）。`dummy.png`は256×128pxで制限内。`git diff --check`と`pdc pd_2048/source pd_2048/builds`に成功した。
- 次の手順: ページ内容に対応するPNGを作成し、各`image`を差し替える。

## PLAYBOOKエディターの検証失敗時終了修正

- 目的: `playbook_editor.py`で保存時のバリデーション失敗後もエディターを継続利用できるようにする。
- 状態: 完了
- 完了事項: バリデーション結果表示をmacOSネイティブ`messagebox.showerror`から、全エラーをスクロール表示するTkinter製モーダルダイアログへ置き換えた。保存・Validateの検証呼び出しへ例外ガードを追加し、失敗時のステータスを表示するようにした。
- 未完了事項: なし。
- 判断: 長い複数行メッセージをネイティブダイアログへ渡さず、アプリ内ダイアログで表示する。予期しない検証例外も保存処理から外へ伝播させず、編集内容を保持する。
- 変更ファイル: `.agents/作業進捗.md`、`仕様書/playbook_editor.py`、`仕様書/プレイブックエディター.md`
- 検証結果: Python構文と`git diff --check`成功。画像未指定の保存を実行し、保存結果が`False`、エディターと検証ダイアログが存続、編集中IDが保持され、ダイアログを閉じた後もフォーム編集できることをTkinter上で確認した。
- 次の手順: なし。

## PLAYBOOKエディター実装

- 目的: `仕様書/プレイブックエディター.md`に従うPython＋Tkinter製`仕様書/playbook_editor.py`を作成する。
- 状態: 完了
- 完了事項: ヒントとページの追加・複製・削除・並べ替え、ID・アンロック番号・日英テキスト編集、同一フォルダPNGの選択・プレビュー、全項目検証、JSONの新規・読込・保存・別名保存、未保存確認、前回ファイル復元を実装した。
- 未完了事項: ゲーム内PLAYBOOK表示、初期`playbooks.json`、説明画像、実績連動は別作業として未実装。
- 判断: 外部依存を増やさず、PNG寸法検証はヘッダー解析、画面プレビューはTkinterの`PhotoImage`で実装した。未完成フォームでも項目移動でき、保存時とValidate時に全エラーをまとめて表示する。未保存JSONでは最初に選んだ画像のフォルダを作業フォルダとして扱う。
- 変更ファイル: `.agents/作業進捗.md`、`仕様書/playbook_editor.py`、`仕様書/プレイブックエディター.md`、`仕様書/playdate_2048_ゲーム仕様書.md`
- 検証結果: Python構文、JSON正規化、アンロック番号、重複ID、画像名、PNGヘッダー・寸法上限の非GUIテストに成功。Tkinter GUIの起動とレイアウトを目視確認。ヒント・ページの追加、複製、削除、並べ替えのGUIテストに成功。実PNGを用いたJSON保存・再読込の往復テストに成功。`git diff --check`成功。
- 次の手順: エディターで初期`playbooks.json`を作成し、説明画像を配置する。

## PLAYBOOK項目・データ・エディタ設計

- 目的: PLAYBOOKのヒント一覧、複数ページ表示、実績連動アンロック、`playbooks.json`、`playbook_editor.py`の仕様を設計する。
- 状態: 完了
- 完了事項: PLAYBOOK一覧・詳細画面の入力と表示、言語フォールバック、JSONスキーマ、画像制約、実績連動、エディタ画面・操作・検証・設定を設計した。初期ヒントとして`基本の遊び方`4ページ、`コンボの作り方`3ページ、`高い数を作る`3ページの日英説明案を定義した。
- 未完了事項: `playbooks.json`、説明画像、PLAYBOOK一覧・詳細表示、アンロック保存、`playbook_editor.py`の実装は未着手。
- 判断: 実績配列の並べ替えで対応が壊れないよう、アンロック番号は実績の表示順ではなく`PLAYBOOK_UNLOCK`報酬IDと照合する独立番号とした。同じ番号を複数ヒントへ設定し、1報酬で関連項目をまとめて解放できる。未解放項目は一覧から隠す。
- 変更ファイル: `.agents/作業進捗.md`、`仕様書/プレイブックエディター.md`、`仕様書/playdate_2048_ゲーム仕様書.md`、`仕様書/実績エディター.md`
- 検証結果: 既存のゲームルール、画面遷移、実績報酬形式と照合し、`git diff --check`に成功した。文書のみの変更のため`pdc`ビルドは実施していない。
- 次の手順: `playbook_editor.py`と初期`playbooks.json`を実装し、説明画像の制作後にゲーム内ローダー・一覧・詳細画面・アンロック保存を実装する。

## 今回の作業

- 目的: タイトル画面から選択できる空の`PLAYBOOK`画面を追加し、Bボタンでタイトルへ戻れるようにする。
- 状態: 完了
- 完了事項: タイトルROOTメニューへ`PLAYBOOK`を追加し、独立した`PLAYBOOK`シーンへ遷移するようにした。画面は`PLAYBOOK`見出しだけを描画し、Bボタンまたはシステムメニューの`Back to Title`でタイトルへ戻る。メニュー背景の対象画面へ追加し、ゲーム仕様書も更新した。
- 未完了事項: Simulatorへの自動キー送信がmacOSのAccessibility権限で拒否されたため、PLAYBOOKへの決定操作とBボタン復帰の動的確認は未実施。
- 判断: 将来の内容追加に備えてタイトル内サブページではなく独立シーンとした。空画面でも画面種別が分かるよう、見出しだけは表示する。
- 変更ファイル: `.agents/作業進捗.md`、`pd_2048/source/app.lua`、`pd_2048/source/game_config.lua`、`pd_2048/source/render/title_renderer.lua`、`pd_2048/source/scene/scene_manager.lua`、`pd_2048/source/scene/scene_playbook.lua`、`pd_2048/source/scene/scene_title.lua`、`仕様書/playdate_2048_ゲーム仕様書.md`
- 検証結果: `git diff --check`成功。`pdc pd_2048/source pd_2048/builds`成功。Simulatorで生成した`source.pdx`を起動し、タイトルメニューへの`PLAYBOOK`表示とコンソールに実行時エラーがないことを確認した。
- 次の手順: 必要に応じてSimulator上で`PLAYBOOK`を決定し、Bボタンでタイトルへ戻る操作を手動確認する。

## 現在の作業

- 目的: リプレイ再生の一時停止地点から、永続記録対象外のプレイヤー操作へ分岐できるようにする。
- 状態: 完了

## 完了事項

- Aで一時停止、停止中のAで確認ダイアログ、`YES`でプレイヤー操作へ分岐する入力仕様を定義した。
- `NO`初期選択、上下選択、A決定、Bキャンセルと、ダイアログ中の入力排他を定義した。
- 安定状態だけでなく、待ち時間・HOLD・DROP・マージ・回転などの途中状態もそのまま引き継ぐ分岐規則を定義した。
- 再生状態と永続記録資格を分離し、分岐後もリプレイ、各種記録、統計、中断データ、将来の実績と報酬を保存しない方針を定義した。
- ゲーム仕様書と入力・リプレイ設計書を更新した。
- `game_state.lua`へ追加する状態と、既存の記録資格を共通の`persistentRecordEligible`へ統合する方針を定義した。
- `game_controller.lua`の分岐関数、入力優先順、再生専用状態の破棄順、永続記録ガードを定義した。
- `overlay_renderer.lua`、`game_renderer.lua`、`scene_game_normal.lua`など、描画とシーンの改修方法を定義した。
- 実装後に確認する分岐地点とdatastore非更新の検証項目を定義した。
- リプレイ一時停止中のAで確認ダイアログを開き、上下選択、A決定、Bキャンセルを処理する入力状態遷移を実装した。
- `YES`決定時に再生専用状態だけを破棄し、盤面、NEXT、HOLD、乱数、UNDO、進行中アニメーションを保持してプレイヤー入力へ切り替える処理を実装した。
- 分岐後にAuto Playとリプレイ記録を停止し、Retryでは通常の新規NORMALへ移行するようにした。
- `persistentRecordEligible`と`canWritePersistentProgress()`を追加し、統計、最高記録、中断保存、リプレイ保存の永続書き込みを共通で抑止した。
- `statisticsRunEligible`と`levelRecordEligible`を廃止し、中断データ形式をバージョン2へ更新した。
- 確認ダイアログ、一時停止操作案内、オーバーレイ優先順、システムメニュー更新を実装した。

## 未完了事項

- Simulatorでの分岐操作とdatastore非更新の動的確認は未実施。Simulatorへの自動キー送信はmacOSのAccessibility権限不足で実行できなかった。
- 前回実装したリプレイ早送りのSimulator確認も未実施。

## 判断・注意事項

- 分岐時にseedから再構築せず、現在の論理状態・乱数状態・UNDO・進行中アニメーションを保持する。
- 未適用のリプレイイベントだけを破棄し、すでに受理済みの操作は取り消さない。
- 分岐後のラン内スコアやレベルは通常どおり進行するが、永続記録資格はラン終了まで復活させない。
- 分岐後は中断保存を許可せず、Retryは新しい記録対象NORMALを開始する。
- 永続記録資格はREWINDで復活させないため、UNDOスナップショットへ含めない。
- 分岐後のゲームオーバーでは`finishReplayRecordingIfNeeded()`がチェックサム検証・中断データ削除・リプレイ保存を行わず、`REPLAY NOT SAVED`を表示しないようにする。
- 以前からの無関係な差分は変更していない。

## 変更ファイル

- `.agents/作業進捗.md`
- `仕様書/playdate_2048_ゲーム仕様書.md`
- `仕様書/入力制御とリプレイ設計.md`
- `pd_2048/source/game_config.lua`
- `pd_2048/source/game_state.lua`
- `pd_2048/source/game/game_controller.lua`
- `pd_2048/source/game/level_progress.lua`
- `pd_2048/source/input/replay_controller.lua`
- `pd_2048/source/render/overlay_renderer.lua`
- `pd_2048/source/render/game_renderer.lua`
- `pd_2048/source/scene/scene_game_normal.lua`

## 検証結果

- 関連するリプレイ入力、手送り、記録抑止、中断保存、結果画面、システムメニューの現行コードを確認した。
- 仕様書内の「続きから通常プレイを開始する機能は提供しない」という旧記述を更新した。
- リプレイ分岐を統計履歴、全記録対象外、中断セーブ、システムメニューの各仕様へ反映した。
- `git diff --check`成功。文書のみの変更のため`pdc`ビルドは実施していない。
- リプレイ再生、通常入力、統計、最高記録、リプレイ完了、中断復元、UNDO、結果画面、システムメニューのLua呼び出し経路を確認した。
- Lua実装方針の追記後に`git diff --check`成功。旧入力案内と旧「続きから開始不可」記述が残っていないことを確認した。
- `statisticsRunEligible`、`levelRecordEligible`、旧`A/B: RESUME`表示がLuaに残っていないことを確認した。
- `pdc pd_2048/source pd_2048/builds`成功。
- Playdate Simulatorで`pd_2048/builds/source.pdx`を起動し、タイトル画面と`REPLAYS`項目が実行時エラーなく表示されることを確認した。
- `osascript`によるSimulatorの自動操作は「キー操作の送信は許可されません」で失敗したため、分岐ダイアログ以降は未確認。

## 次の手順

- 分岐前後、アニメーション途中、手送り後、ゲームオーバー、Retry、システムメニュー、記録非更新をSimulatorで確認する。

## 前回作業の継続確認

- リプレイ再生中、Bボタン押下中は最大3000msの記録済み操作待ちだけを省略し、アニメーション速度は変更しない実装まで完了している。
- `git diff --check`と`pdc pd_2048/source pd_2048/builds`は成功済み。Simulatorでの動的確認が残っている。
